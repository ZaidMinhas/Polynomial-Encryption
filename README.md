# Polynomial-Encryption
This is an encryption algorithm that takes coefficents a,b,c,d to encrypt text

There are already ab,c,d values set in the code but if values are changed then you are required to remember them
